/*
package other.DriverFactory;

import other.StepsPattern.steps.SPSearchSteps;
import org.openqa.selenium.WebDriver;
import org.testng.annotations.BeforeClass;

public class ADFBaseTest {

    private static WebDriver driver;

    @BeforeClass
    public void setUp() {

        driver = other.DriverFactory.getDriver(DFBrowser.CHROME);
        driver.get("https://www.google.com");
        steps = new SPSearchSteps();
    }
}
*/
